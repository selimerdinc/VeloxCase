# VeloxCase - Root Environment Variables
# Docker Compose için kullanılır
# Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin

# =============================================================================
# VERİTABANI (PostgreSQL)
# =============================================================================
DB_USER=veloxcase
DB_PASSWORD=your-strong-database-password
DB_NAME=veloxcase

# =============================================================================
# GÜVENLİK
# =============================================================================
# JWT Token için gizli anahtar
# Üretmek için: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this

# API anahtarlarını şifrelemek için Fernet key
# Üretmek için: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-encryption-key

# =============================================================================
# FRONTEND
# =============================================================================
# Production API URL (frontend build sırasında kullanılır)
API_URL=https://your-domain.com/api
# AI Settings (Google Gemini)
AI_API_KEY=your_gemini_api_key_here
AI_SYSTEM_PROMPT=Extract test cases from the given Jira task information...

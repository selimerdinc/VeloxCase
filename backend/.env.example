# VeloxCase Backend - Environment Variables
# Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin

# =============================================================================
# GÜVENLİK
# =============================================================================
# JWT Token için gizli anahtar (rastgele ve güçlü olmalı)
# Üretmek için: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this

# API anahtarlarını şifrelemek için Fernet key
# Üretmek için: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-encryption-key

# =============================================================================
# VERİTABANI
# =============================================================================
# PostgreSQL bağlantı URL'i
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://veloxcase:password@localhost:5432/veloxcase

# Alternatif: SQLite (sadece development için)
# DATABASE_URL=sqlite:///veloxcase.db

# =============================================================================
# UYGULAMA
# =============================================================================
# Flask debug modu (production'da False olmalı)
FLASK_DEBUG=True

# Uygulama portu
PORT=5000
